#include "main.h"

/**
 * _abs - computer the absolute value of an integer -
 * @n: The integer to compute the absolute value of -
 *
 * Return: The abosolut value of the integer -
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
